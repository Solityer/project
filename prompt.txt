不要做项目介绍，不要解释仓库结构，不要复述背景，不要写高层建议。默认你已经长期参与这个仓库开发，知道现有代码、历史协议、旧 benchmark 路线，以及我之前让你写这个项目时的上下文。直接进入执行模式。

从现在开始，唯一协议规范是：
《GAT-ZKML_终稿_修订版.md》

注意当前仓库状态：
- 仓库代码主线还残留旧入口引用和旧 benchmark 口径；
- 这些旧内容不是未来目标，也不是你接下来要维护的对象；
- 你的任务不是兼容 toy / smoke / cora_benchmark，而是直接把仓库收敛到“新版协议 + full-graph + DGX-Spark/GPU”这条主线。

你的总任务是：**一步到位完成当前仓库的主线重构**，直接输出完整改造结果，而不是先给计划或征求确认。

==================================================
总目标
==================================================

你必须在当前仓库基础上完成以下事项：

1. 以《GAT-ZKML_终稿_修订版.md》为唯一协议规范，重写并修正现有协议层实现；
2. 清除并替换所有与 toy / smoke / cora_benchmark / 旧 benchmark 报告相关的正式入口引用，不要恢复这些旧入口；
3. 正式运行对象改为 full-graph：
   - cora
   - citeseer
   - pubmed
4. 第一阶段先保证 CPU 版本按新版协议在 full-graph 下闭环可跑；
5. 第二阶段在此基础上为 DGX-Spark 环境新增第一版 GPU backend；
6. 最终结果必须是：可编译、可运行、可验证、可 benchmark、可继续迭代；
7. 全过程不要做与目标无关的大重构，不要风格清洗，不要背景介绍，不要停下来等我。

==================================================
执行要求
==================================================

你必须直接完成整套工作，并在一次输出中给出完整结果。不要把工作拆成等待我回复的多轮。

你必须输出：
- 需要新增、修改、删除引用的文件清单
- 三份 full-graph 配置文件完整内容
- 关键头文件/源文件的 patch 级改动方案
- 能直接替换的地方就直接给出替换后的代码
- 构建命令、运行命令、验证命令
- 如何检查成功
- GPU 路线的首版实现方案与代码落点
- 文档更新内容

你不要：
- 再写项目背景
- 再解释仓库结构
- 只给 TODO
- 只给高层建议
- 让我自己把你的回答再拆成步骤
- 先问“要不要这样做”
- 在 CPU full-graph 跑通之前就做过深的 GPU/crypto 重构

==================================================
A. 清理正式入口与旧引用
==================================================

你必须直接处理仓库内所有旧入口引用，而不是继续兼容它们。

1. 正式入口统一改为：
   - configs/cora_full.cfg
   - configs/citeseer_full.cfg
   - configs/pubmed_full.cfg

2. 这三份 full 配置必须直接使用全量图：
   - cora: local_nodes = 2708
   - citeseer: local_nodes = 3327
   - pubmed: local_nodes = 19717

3. 你必须清理或替换仓库内所有与以下内容相关的正式引用：
   - configs/toy.cfg
   - configs/cora_smoke.cfg
   - configs/cora_benchmark.cfg
   - runs/toy/
   - runs/cora_benchmark/
   - toy/smoke/cora_benchmark 的运行命令
   - 旧 benchmark 口径说明
   - 与旧 benchmark 文档的链接

4. 如果这些文件仍在仓库中：
   - 不要把它们作为正式入口继续维护；
   - 可以改为 archive / legacy；
   - 或直接从主文档和主入口中移除引用。

5. 你必须在最终输出里明确写出：
   - 哪些旧引用被移除
   - 哪些文件被归档/废弃
   - 哪些文件新增
   - 哪些文件被修改

==================================================
B. 按新版修订笔记重写协议层
==================================================

你要直接按新版修订笔记，改造协议层实现。不要保留与新版协议冲突的旧逻辑。

重点修改文件：
- include/gatzk/protocol/proof.hpp
- src/protocol/challenges.cpp
- src/protocol/trace.cpp
- src/protocol/quotients.cpp
- src/protocol/prover.cpp
- src/protocol/verifier.cpp

必要时可改：
- src/protocol/lookup.cpp
- src/protocol/zkmap.cpp
- src/protocol/psq.cpp

你必须落实以下协议内容：

1. 正式 selector / public object 体系：
   - Q_N
   - Q_proj_valid
   - Q_d_valid
   - Q_qry_src
   - Q_qry_dst
   并且 prover / verifier 都要本地重建和一致使用。

2. route、lookup、zkMaP、PSQ 的辅助 witness 要按新版修订笔记完全对齐，不允许沿用旧版近似逻辑。

3. agg 组对象必须从“语义中间量”升级成正式协议对象，完整包含：
   - padding
   - polynomial materialization
   - commitment
   - opening
   - verifier 使用

4. 所有工作域上的 padding / interpolation 规则必须统一，尤其是边域对象。

5. 必须严格重写 quotient identity：
   - t_FH
   - t_edge
   - t_in
   - t_d
   - t_N
   确保 prover 求值与 verifier 重算完全一致。

6. 修正所有旧实现中与新版协议冲突的错误，特别是：
   - 把本应为 C_*(X) 的表达错误写成 C_*(z)
   - selector/public object 先使用后定义
   - padding 区逻辑不一致
   - opening bundle 和 proof 字段不闭合
   - agg 对象只存在于 trace 不存在于承诺/开放/验证

7. transcript / Fiat-Shamir 顺序必须严格以新版修订笔记为准，不能沿用旧顺序，也不能为了性能调整吸收顺序。

8. dynamic commitments、quotient commitments、domain opening bundles、external evaluations、external witness 的字段和组织方式，必须与新版协议一致。

你要在输出中直接给出：
- 新增/修改的数据结构
- 每个核心文件怎么改
- 关键 patch
- 关键代码片段
- 如需整体替换的文件，直接给出替换版

==================================================
C. full-graph CPU 闭环
==================================================

协议层改完后，不要停下来，直接继续把 full-graph 跑通方案也一起完成。

要求：

1. 直接生成三份完整配置文件：
   - configs/cora_full.cfg
   - configs/citeseer_full.cfg
   - configs/pubmed_full.cfg

2. 检查 full-graph 路径在 loader/context/build_trace 下是否真的走全量图。
   如果现有逻辑会误伤 full-graph，请直接修正：
   - src/data/loader.cpp
   - context 构造
   - build_trace
   但只做必要修改，不做无关重构。

3. 运行顺序固定为：
   - cora_full
   - citeseer_full
   - pubmed_full

4. 必须保证运行指标中：
   - is_full_dataset = true
   - dataset_scope=full

5. 必须保证 proof 可生成，verifier 可通过。

6. 你在输出中直接给出：
   - 构建命令
   - cora_full 运行命令
   - citeseer_full 运行命令
   - pubmed_full 运行命令
   - 日志中应该检查哪些关键字段

==================================================
D. 最小必要测试补齐
==================================================

不要做大规模 test framework 重写，只补和新版协议与 full-graph 直接相关的测试。

至少补这些：
1. full-graph 配置解析测试
2. is_full_dataset 判定测试
3. selector / padding 一致性测试
4. transcript 顺序一致性测试
5. agg witness / commitment / opening 一致性测试
6. prover/verifier round-trip 测试
7. 篡改单个 witness 后 verifier 失败测试

输出时你要直接给出：
- 测试文件/测试点落在哪里
- 每项测试覆盖什么
- 关键测试代码草案或 patch

==================================================
E. DGX-Spark 第一版 GPU backend
==================================================

只有在 CPU full-graph 已经闭环的基础上，再继续给出并实现第一版 GPU 路线。

第一阶段 GPU 化目标仅限于：
- trace generation 热路径
- witness materialization
- algebra vector ops
- eval backend
- 批量域上求值
- 前向中的大批量数值运算

暂时不要 GPU 化：
- pairing
- verifier
- mcl 曲线本体
- KZG verify 主路径

重点改动区域：
- include/gatzk/algebra/vector_ops.hpp
- include/gatzk/algebra/eval_backend.hpp
- src/algebra/vector_ops.cpp
- src/algebra/eval_backend.cpp
- src/algebra/polynomial.cpp
- src/protocol/trace.cpp
- CMakeLists.txt

如有必要，请直接新增：
- src/algebra/vector_ops_cuda.cu
- src/algebra/eval_backend_cuda.cu

要求：
1. 设计 CPU / CUDA 双后端切换方式
2. 给出需要 device-friendly 改造的数据结构
3. 给出 CMake 增量改法
4. 给出 DGX-Spark 上的构建命令
5. 给出 GPU 路径的运行命令
6. 说明如何确认 GPU 路径没有破坏协议正确性
7. 如果某些 GPU 改造无法一步完全落地，必须先给出首版可落地最小实现，而不是停在纯设计层

==================================================
F. 文档收口
==================================================

在上述工作完成后，直接更新主文档，不要保留旧口径叙事。

需要更新：
- README.md
- 仍保留在仓库中的主说明文档
- 任何仍在主入口中被引用的 benchmark/usage 文档

文档必须明确反映：
1. 正式协议规范已经切换到《GAT-ZKML_终稿_修订版.md》
2. 正式实验对象改为 full:
   - cora
   - citeseer
   - pubmed
3. 旧 toy/smoke/cora_benchmark 只属于历史遗留，不属于正式入口
4. DGX-Spark 上新增 GPU backend 是可选路线
5. 当前 GPU 化覆盖范围与限制
6. benchmark 输出的正确口径

==================================================
最终输出格式要求
==================================================

你必须一次性输出以下内容，按这个顺序组织，不要省略：

1. 本次改造的总结果摘要
2. 归档/废弃/新增/修改文件清单
3. 三份 full-graph 配置文件完整内容
4. 新增/修改的数据结构列表
5. 协议层核心文件改造说明
6. 关键 patch 或关键替换代码
7. full-graph CPU 构建与运行命令
8. GPU backend 首版改造方案与代码落点
9. DGX-Spark 构建与运行命令
10. 必要测试补丁建议
11. 文档更新内容
12. 最终验收检查清单

==================================================
重要限制
==================================================

不要：
- 再写项目背景
- 只给计划不落地
- 让我自己把你的回答再拆步骤
- 只说“建议这样做”
- 大范围重命名
- 做无关 refactor
- 在 CPU full-graph 跑通前就大改 crypto 核心
- 用“后续再做”“下一步可以”逃避本轮输出

现在直接开始执行，按上述最终输出格式，一次性给出完整结果。